╔══════════════════════════════════════════════════════════════════════╗
║                     🚀 DEPLOY YOUR APP ONLINE!                        ║
╚══════════════════════════════════════════════════════════════════════╝

Your app is ready to go live! Follow these simple steps:

┌─────────────────────────────────────────────────────────────────────┐
│ STEP 1: Login to Vercel (1 minute)                                 │
└─────────────────────────────────────────────────────────────────────┘

Run this command:
  $ vercel login

Your browser will open → Click "Continue with GitHub" → Done!

┌─────────────────────────────────────────────────────────────────────┐
│ STEP 2: Deploy Backend API (2 minutes)                             │
└─────────────────────────────────────────────────────────────────────┘

  $ cd backend
  $ vercel --prod

When prompted, press Enter for all questions (use defaults).
You'll get a URL like: https://babysu-backend-abc123.vercel.app

COPY THIS URL! You'll need it for Step 4.

┌─────────────────────────────────────────────────────────────────────┐
│ STEP 3: Add API Keys to Backend (3 minutes)                        │
└─────────────────────────────────────────────────────────────────────┘

Run these commands (paste your keys when prompted):

  $ vercel env add OPENAI_API_KEY production
  $ vercel env add GEMINI_API_KEY production
  $ vercel env add SUNO_API_KEY production
  $ vercel env add JWT_SECRET production
  $ vercel env add FIREBASE_PROJECT_ID production
  $ vercel env add FIREBASE_STORAGE_BUCKET production
  $ vercel env add CORS_ORIGIN production

Then redeploy:
  $ vercel --prod

┌─────────────────────────────────────────────────────────────────────┐
│ STEP 4: Deploy Web App (2 minutes)                                 │
└─────────────────────────────────────────────────────────────────────┘

  $ cd ../webapp
  $ vercel --prod

You'll get a URL like: https://babysu-webapp-xyz789.vercel.app

┌─────────────────────────────────────────────────────────────────────┐
│ STEP 5: Add Config to Webapp (2 minutes)                           │
└─────────────────────────────────────────────────────────────────────┘

  $ vercel env add VITE_API_URL production
  (Paste your backend URL from Step 2)

  $ vercel env add VITE_FIREBASE_API_KEY production
  $ vercel env add VITE_FIREBASE_PROJECT_ID production
  (Add other Firebase variables - see QUICK_DEPLOY.md)

Then redeploy:
  $ vercel --prod

┌─────────────────────────────────────────────────────────────────────┐
│ ✅ DONE! Open your app in a browser!                               │
└─────────────────────────────────────────────────────────────────────┘

Your live webapp URL: https://babysu-webapp-xyz789.vercel.app

═══════════════════════════════════════════════════════════════════════

📚 DETAILED GUIDES:

  • QUICK_DEPLOY.md    - Step-by-step with all commands
  • DEPLOYMENT_GUIDE.md - Complete guide with troubleshooting
  • ENV_STATUS_REPORT.md - Security status of your .env files

═══════════════════════════════════════════════════════════════════════

💡 TIPS:

  ✓ Total time: ~10 minutes
  ✓ You can deploy from any computer with your GitHub account
  ✓ Free tier includes HTTPS, CDN, and auto-scaling
  ✓ To update: just run "vercel --prod" again after making changes

═══════════════════════════════════════════════════════════════════════

🆘 NEED HELP?

  • Check deployment logs: vercel logs
  • View environment variables: vercel env ls
  • Remove and re-add variables if needed
  • See DEPLOYMENT_GUIDE.md for troubleshooting

═══════════════════════════════════════════════════════════════════════
